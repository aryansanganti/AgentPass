# AgentPass — Demo Script

> This is the single narrative from PRD 00 Section 5. Use this for ALL demo videos.

---

## Main Demo Narrative (~2 minutes)

*"Meet AgentPass. It's an AI agent with its own name — sentinel.agentpass.eth — registered on ENS. Before I let it spend anything, it proves it's working for a real human, me, via World ID.*

*Now watch: I ask it to check my DeFi risk. It pulls my live lending positions from The Graph across two protocols, computes a risk score, then — instead of using an API key — it pays for its own analysis with testnet HBAR, live, on Hedera, through x402.*

*Here's the transaction. Here's the result: I'm at 72% risk, mostly from a high LTV position on Aave.*

*And because this agent is registered on Bazantic with a Recipe, any other AI agent can discover and use it too — here's one doing exactly that, getting a measurably better result because the Recipe exists."*

---

## Per-Bounty Short Clips

For each specific bounty submission, record a **shorter, focused clip** zooming into just that component — reusing footage from the same running app:

1. **ENS** (~1 min): Focus on the Agent Profile screen, subname hierarchy, role-based permissions
2. **The Graph** (~1 min): Focus on live MCP query → risk engine reasoning
3. **Hedera** (~2 min): Focus on the 402 → payment → retry → receipt flow
4. **World** (~1 min): Focus on the verification gate blocking then unlocking
5. **Bazantic** (~1 min): Focus on the A/B comparison artifact
